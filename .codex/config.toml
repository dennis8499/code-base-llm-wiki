[features]
hooks = true

[wiki_guard]
# Installed repos use "wiki-only" by default or explicit "coexist" for normal
# coding sessions. Use "framework" only in this framework repository.
mode = "framework"

[agents]
max_threads = 6
max_depth = 1
