interface:
  display_name: "需求探索"
  short_description: "探索需求，以證據與一次一題收斂可驗收規格"
  default_prompt: "使用 $requirements-discovery，以證據先行、一次一題的 frontier 訪談探索這項需求，直到形成可確認的需求分析。"

policy:
  allow_implicit_invocation: true
