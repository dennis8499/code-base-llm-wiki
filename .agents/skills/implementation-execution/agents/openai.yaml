interface:
  display_name: "實作執行"
  short_description: "執行 Ready 計畫，完成 BDD／TDD、驗證與 fresh review"
  default_prompt: "使用 $implementation-execution，依指定的 ready-plan/v1 執行工作包、全量驗證並取得 fresh 唯讀 review。"

policy:
  allow_implicit_invocation: true
