"""Shared Codebase LLM Wiki runtime.

The runtime is deliberately independent from either agent surface. Copilot
and Codex adapters call the same CLI and JSON contract.
"""

__version__ = "0.1.0"
CONTRACT_VERSION = 1
